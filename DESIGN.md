# 五子棋网页游戏 — 设计文档

> 日期：2026-08-15 ｜ 状态：待用户审查 ｜ 项目根：`gomoku/`

## 1. 需求定稿（已与用户确认）

| 需求项 | 定稿内容 |
|---|---|
| 对战模式 | ① 人机对战（一至九段 AI）② 局域网联机 ③ 互联网联机 |
| 联机架构 | **无服务器 WebRTC 方案**（用户已选）：PeerJS + 公共信令（可配置），零后端 |
| 工程形态 | **原生 HTML/CSS/JS 单页，无构建**（用户已选）：双击 index.html 即可跑 |
| 界面风格 | **墨色极简·现代扁平**（用户已选）：深色底 + 黑白棋子 + 琥珀金强调色 |
| 核心规则 | 15×15 棋盘，黑白轮流，任意方向五连获胜；悔棋 / 认输 / 重新开局 / 观战 |
| 附加功能 | 联机实时聊天、断线重连、对局历史（含回放）；落子音效、胜负提示、对局计时 |
| 界面要求 | 响应式（桌面 / 移动端），简洁美观 |

## 2. 整体架构

```
┌────────────────────────────── 浏览器 A（房主/黑方）─────────────────────────────┐
│  index.html                                                                      │
│  ├─ css/style.css          墨色极简主题，响应式布局                              │
│  ├─ js/engine.js           棋盘状态 + 规则 + 胜负判定（纯逻辑，无 DOM）          │
│  ├─ js/ai.js               评分函数 + α-β 剪枝 + 九段难度配置                    │
│  ├─ js/net.js              PeerJS 封装：房间/协议/重连/聊天/观战                 │
│  ├─ js/audio.js            Web Audio 合成音效（零外部文件）                      │
│  ├─ js/storage.js          localStorage 对局历史 + 回放                          │
│  └─ js/app.js              视图路由 + UI 绑定 + 计时                              │
└──────────────┬───────────────────────────────────────────────┬──────────────────┘
               │ WebRTC DataChannel（P2P，不经过服务器转发数据） │
               │ 信令（仅交换 SDP/ICE）：PeerJS 公共信令，地址可配置 │
               ▼                                               ▼
┌────────────────────────────── 浏览器 B（加入者/白方）──┐   ┌── 浏览器 C…N（观战者）─┐
│  同构单页应用，角色由握手消息分配                      │   │  只读订阅广播          │
└───────────────────────────────────────────────────────┘   └───────────────────────┘
```

**关键决策：**
- **信令可配置**：默认 PeerJS 官方公共信令（`0.peerjs.com`）；`index.html` 顶部常量 `PEER_CONFIG` 可切换自托管信令（局域网内一条 `npx peer` 命令即可），解决公共信令不可达问题。
- **host 权威模型**：房主 = 权威节点。所有落子由 host 校验合法性后广播；观战者、白方均以 host 状态为准；避免双端状态分歧。
- **确定性房间 ID**：房间码 4 位（字母+数字，如 `K3FQ`），Peer ID = `gomoku-<房间码>`。加入者直接 `connect` 该 ID，无需信令服务器之外的中介。
- **普通 script 标签 + 全局命名空间**（`window.Gomoku.*`），不用 ES Modules——保证 `file://` 双击也能跑（ESM 在 file:// 下有 CORS 限制）。
- **PeerJS 双 CDN fallback**：jsdelivr 主 + unpkg 备（国内可达性差异），加载失败自动切换。

## 3. 联机协议（DataChannel JSON 消息）

| 消息 | 方向 | 字段 | 说明 |
|---|---|---|---|
| `hello` | 加入者 → host | `role: 'player'\|'spectator'`, `name` | 握手；host 按当前席位分配（黑=房主，白=首个玩家，其余观战） |
| `welcome` | host → 加入者 | `seat`, `board`, `turn`, `seq`, `players` | 完整状态快照（含重连恢复场景） |
| `move` | 任意方 → host；host → 全员 | `x, y, seq` | 落子；host 校验合法后广播；观战者也收 |
| `chat` | 任意 → 全员 | `text, from, ts` | 实时聊天，广播 |
| `undo-req` / `undo-ok` / `undo-no` | 双向 | — | 悔棋需对方同意（联机模式） |
| `resign` | 任意 → 全员 | `seat` | 认输 |
| `restart-req` / `restart-ok` | 双向 | — | 重新开局需对方同意 |
| `ping` / `pong` | 双向 | `ts` | 心跳，30s 无响应判定断线 |
| `state-sync` | host → 重连方 | 同 `welcome` | 断线重连的状态恢复 |

**断线重连**：Peer 断开后，host 保留该席位 **90 秒**；玩家用相同房间码重连，`hello` 中携带原 `seat`，host 比对后发 `state-sync` 恢复棋局。超时未归则该席位判负（观战者直接移除）。
**观战**：第 3 个及以后连接自动为观战者，只收广播不可落子。
**计时**：host 权威计时，双端显示双方累计用时；断线期间暂停本人计时。

## 4. AI 设计（九段难度）

- **评分表**（对每个位置四方向扫描）：五连 1000000 / 活四 100000 / 冲四 10000 / 活三 1000 / 眠三 100 / 活二 10 / 眠二 1；黑白分算后相减。
- **搜索**：α-β 剪枝 + 候选点裁剪（只评估距离已有棋子 ≤2 格的点，按启发分排序），带**时间上限**（默认 1.5s，超时返回当前最优）。
- **九段映射**：

| 段位 | 搜索深度 | 随机扰动 | 说明 |
|---|---|---|---|
| 1-3 段 | 1（贪心） | 30%-10% | 会挡会冲但常失误，新手友好 |
| 4-6 段 | 2 | 5%-1% | 稳定攻防，一般玩家势均力敌 |
| 7-9 段 | 3-4（迭代加深） | 0.5%-0% | 强攻强防，接近人机最强 |

- 必杀快速判定：轮到谁时先查"一步五连 / 挡一步五连"，段位 ≥4 必执行，低段位概率触发（制造"会但会漏"的拟人感）。

## 5. UI 设计方向（墨色极简·现代扁平）

- **配色**：背景 `#0f1115`（近黑），面板 `#171a21`，边框 `#2a2e38`；白棋 `#f2f0ea`，黑棋 `#1a1c22`（棋盘深色底上黑棋用浅色描边区分）；强调色琥珀金 `#d4a853`（按钮主色 / 赢家高亮 / 选中态）。
- **布局**：桌面 = 左侧棋盘主区 + 右侧控制栏（模式选择 / 玩家信息 / 聊天 / 历史）；移动端 = 纵向堆叠，棋盘优先，聊天折叠。
- **棋盘**：Canvas 渲染（DPR 适配，响应式最大边 640px），落子有轻微涟漪动画；坐标无格网噪声，极简。
- **首页视图**：三张模式卡片（人机 / 局域网 / 互联网），纯文字 + 图标，无装饰图。
- **音效**：Web Audio 合成（落子"嗒"、胜利琶音、聊天提示），无外部文件。

## 6. 文件结构与交付

```
gomoku/
├── index.html         入口（视图骨架 + 脚本引入 + CDN fallback）
├── css/style.css      墨色主题 + 响应式
├── js/engine.js       核心规则引擎（纯逻辑）
├── js/ai.js           AI 九段
├── js/net.js          联机（PeerJS）
├── js/audio.js        合成音效
├── js/storage.js      历史 + 回放
├── js/app.js          UI 总控
└── README.md          启动 / 部署 / 自托管信令说明
```

## 7. 启动与部署（README 将覆盖）

1. **本地**：双击 `index.html`（联机功能依赖网络访问信令 CDN）；或 `python -m http.server 8000` 起静态服务。
2. **局域网联机**：同一局域网，一方建房发房间码，另一方加入即可（数据走 P2P 直连）；若公共信令不可达，任一方起自托管信令并改 `PEER_CONFIG`。
3. **互联网**：静态页部署到任意托管（GitHub Pages / CloudStudio 等）；联机数据仍 P2P，仅信令经公共服务器。**注意**：无服务器方案下无法做"全球匹配池"，匹配 = 房间码分享（跨网络可连，靠分享房间码，非自动撮合）。

## 8. 已知取舍（诚实声明）

- "互联网联机" = 跨网络房间码连接（无服务器架构的固有形态），非中央匹配池；若未来要自动匹配需引入服务器，架构预留（`net.js` 协议层独立，可替换为 WebSocket 后端）。
- 联机悔棋/重开需双方同意（避免单方操控）。
- 断线重连有 90s 席位保留窗口。
- 观战人数无上限（P2P 扇形广播，host 单点扇出）。
